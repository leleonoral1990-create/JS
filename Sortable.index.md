| Строка кода | Для чего она нужна (простым языком) | Важные нюансы (синтаксис) | Пример использования еще |
| :--- | :--- | :--- | :--- |
| `<!DOCTYPE html>` | Говорит браузеру (Chrome, Safari): "Эй, я написан на самом современном HTML5, читай меня правильно". | Пишется всегда в самой 1-й строке. У этого тега нет "закрывающей пары". | *(Всегда пишется именно так)* |
| `<html lang="en">` | Самый главный тег — "контейнер" всего сайта. `lang="en"` подсказывает поисковикам, что сайт на английском. | Это открывающий парный тег. В самом конце файла обязательно будет его пара `</html>`. | `<html lang="ru">` (для русского) |
| `<head>` | Открывает блок невидимых "настроек" страницы. Пользователь это не видит, это инфа для браузера. | Парный тег (закроем его позже). Здесь живут скрипты, шрифты и дизайн. | *(Стандартный тег)* |
| `<meta charset="UTF-8">` | Включает кодировку UTF-8. Без неё русские буквы, иероглифы и эмодзи превратятся в "кракозябры". | Одинарный тег (его не нужно закрывать слэшем `/`). | *(Стандарт, всегда копируется как есть)* |
| `<meta name="viewport" content="width=device-width, initial-scale=1.0">` | Делает сайт адаптивным. Заставляет сайт нормально выглядеть на мобильных телефонах, а не быть мелким. | Всегда добавляй эту строку, если хочешь, чтобы с телефона сайт читался нормально. | *(Стандарт, всегда копируется как есть)* |
| `<title>Villain's Sortable Database</title>` | Название страницы. Именно этот текст ты видишь на "вкладке" браузера наверху. | Текст пишется строго между открывающим `<title>` и закрывающим `</title>`. | `<title>Мой первый сайт</title>` |
| `<link rel="stylesheet" href="style.css">` | Подключает файл с дизайном (CSS). Именно он сделает сайт красивым (цвета, рамки). | `href` — это путь к твоему файлу. Одинарный тег. | `<link rel="stylesheet" href="design.css">` |
| `<script src="main.js" defer></script>` | Подключает файл с логикой JavaScript. Именно он "оживит" кнопки и поиск. | Слово **`defer`** — суперважное! Оно приказывает JS подождать, пока не загрузится весь HTML, и только потом работать. | `<script src="app.js" defer></script>` |
| `</head>` | Закрывает блок невидимых настроек. | Обязательный шаг перед началом видимой части сайта. | *(Стандартный тег)* |
| `<body>` | **Начало видимой части!** Всё, что внутри этого тега, пользователь увидит на экране. | Парный тег. Будет закрыт в самом низу файла перед `</html>`. | *(Стандартный тег)* |
| `<header>` | Создает "шапку" сайта. Это просто коробка для логического порядка (чтобы отделить меню от контента). | Внутри обычно кладут логотипы, заголовки и панели поиска. | *(Стандартный тег)* |
| `<h1>Super-Smartypants Tracker</h1>` | Самый главный, крупный заголовок на странице (Заголовок 1 уровня). | Хорошее правило: на странице должен быть только один `<h1>`. | `<h1>Список моих задач</h1>` |
| `<div class="toolbar">` | `<div>` — это универсальная "коробка". А `class="toolbar"` — это бирка, чтобы мы могли в CSS сказать: "Выстрой всё в этой коробке в один ряд". | Классы (`class`) нужны для дизайна (CSS). Их может быть много одинаковых на странице. | `<div class="menu-box">` |
| `<input type="text" id="q" placeholder="Search name (use '-' to exclude)...">` | Поле, куда юзер будет вводить текст (поиск). | `id` ("ай-ди") — это уникальный паспорт элемента. По `id="q"` твой JavaScript найдет это поле. `placeholder` — исчезающая подсказка. | `<input type="password" placeholder="Пароль">` |
| `<label>Show: ` | Текстовая обертка ("Показывать:"). Связывает текст и выпадающий список ниже. | Если кликнуть по слову внутри `<label>`, активируется элемент внутри него. | `<label>Имя: <input></label>` |
| `<select id="size">` | Создает выпадающий список. По `id="size"` твой JS поймет, сколько строк показывать. | Внутри него обязательно должны лежать пункты — теги `<option>`. | `<select id="color">...</select>` |
| `<option value="10">10</option>` | Пункт списка. | `value="10"` — это скрытое значение для JS. А `10` между тегами — то, что видит юзер. | `<option value="red">Красный</option>` |
| `<option value="20" selected>20</option>` | Второй пункт списка. | Слово `selected` делает этот вариант выбранным по умолчанию сразу при открытии сайта. | `<option value="ru" selected>Русский</option>` |
| `<option value="50">50</option>` | Третий пункт списка (показывать 50 строк). | Синтаксис аналогичный первой опции. | `<option value="cat">Кот</option>` |
| `<option value="100">100</option>` | Четвертый пункт списка (показывать 100 строк). | Синтаксис аналогичный первой опции. | `<option value="dog">Собака</option>` |
| `<option value="all">All</option>` | Пятый пункт (показать всё). | Значение `value` может быть не только цифрой, но и текстом (`"all"`). | `<option value="none">Скрыть</option>` |
| `</select>` | Закрывает выпадающий список. | Пара для тега `<select>`. | *(Стандартный тег)* |
| `</label>` | Закрывает текстовую обертку. | Пара для тега `<label>`. | *(Стандартный тег)* |
| `<button id="themeToggle">🌓 Switch Theme</button>` | Кнопка на экране. | По `id="themeToggle"` скрипт поймет, что при клике сюда надо менять цвет (светлый/темный). | `<button id="saveBtn">Сохранить</button>` |
| `</div>` | Закрывает "коробку" панели управления (`toolbar`). | Внимательно следим, какую коробку открыли последней, ту закрываем первой. | *(Стандартный тег)* |
| `</header>` | Закрывает логический блок "шапки" сайта. | Дальше пойдет основной контент страницы. | *(Стандартный тег)* |
| | | | |
| `<div class="table-container">` | Ещё одна "коробка", в которую мы положим таблицу. | Полезна для CSS: если таблица будет слишком широкой, эта коробка позволит добавить ей полосу прокрутки. | `<div class="gallery">` |
| `<table>` | Создает саму таблицу. | Главный контейнер для всех строк и столбцов. | *(Стандартный тег)* |
| `<thead id="headers">` | Создает "голову" (шапку) таблицы. Здесь лежат названия колонок. | `id="headers"` позволит JS кликать по этим заголовкам, чтобы сортировать таблицу. | *(Стандартный тег)* |
| `<tr>` | Создает одну строку таблицы (Table Row). | Таблица строится горизонтальными строками. | *(Стандартный тег)* |
| `<th>Icon</th>` | Ячейка-заголовок (Table Heading). Текст внутри будет жирным. | Обычная колонка без дополнительных свойств. | `<th>Фото</th>` |
| `<th data-col="name">Name</th>` | Ячейка с "секретом" для JS. | Атрибут `data-col` придуман тобой! HTML он не нужен, но JS прочтет `"name"` и поймет, как отсортировать базу по именам. | `<th data-type="price">Цена</th>` |
| `<th data-col="biography.fullName">Full Name</th>` | Заголовок "Полное имя". | Ключ сортировки здесь сложнее: `"biography.fullName"` — путь к данным внутри объекта в JS. | `<div data-id="123">` |
| `<th data-col="powerstats">Stats</th>` | Заголовок "Статистика". | - | - |
| `<th data-col="appearance.race">Race</th>` | Заголовок "Раса". | - | - |
| `<th data-col="appearance.gender">Gender</th>` | Заголовок "Пол". | - | - |
| `<th data-col="appearance.height">Height</th>` | Заголовок "Рост". | - | - |
| `<th data-col="appearance.weight">Weight</th>` | Заголовок "Вес". | - | - |
| `<th data-col="biography.placeOfBirth">Place of Birth</th>` | Заголовок "Место рождения". | - | - |
| `<th data-col="biography.alignment">Alignment</th>` | Заголовок "Принадлежность" (добро/зло). | - | - |
| `</tr>` | Закрывает строку с заголовками. | - | *(Стандартный тег)* |
| `</thead>` | Закрывает "голову" таблицы. | - | *(Стандартный тег)* |
| `<tbody id="out">` | Создает "тело" таблицы, где будут сами данные. | Это **мишень для твоего JavaScript**. По `id="out"` скрипт найдет это место и вставит сюда десятки строк с героями. | `<tbody id="usersList">` |
| `<tr><td colspan="10" class="status">Hacking database... ⏳</td></tr>` | Строка-заглушка. Она показывает текст загрузки, пока JS тянет данные. | `<td>` — это обычная ячейка. `colspan="10"` говорит ячейке: "Растянись на ширину всех 10 колонок сверху!". | `<td colspan="2">Итого</td>` |
| `</tbody>` | Закрывает тело таблицы. | - | *(Стандартный тег)* |
| `</table>` | Закрывает саму таблицу. | - | *(Стандартный тег)* |
| `</div>` | Закрывает "коробку" `table-container`. | - | *(Стандартный тег)* |
| | | | |
| `<div id="nav" class="pagination"></div>` | Пустая коробка для кнопок страниц (1, 2, 3, Вперед...). | Сейчас она пустая. JS сам создаст кнопочки и положит их сюда по `id="nav"`. | `<div id="sliderControls"></div>` |
| | | | |
| `<div id="modal" class="modal" onclick="closeHero()">` | Это "всплывающее окно" (попап) — темный полупрозрачный фон. | `onclick="closeHero()"` означает: "Если кликнуть мышкой на этот темный фон, запусти JS функцию `closeHero` (которая закроет окно)". | `<div onclick="alert('Привет!')">` |
| `<div class="modal-content" onclick="event.stopPropagation()">` | Это сама белая карточка с инфой, которая лежит поверх темного фона. | `event.stopPropagation()` — это **щит**! Он запрещает клику "пробивать" карточку насквозь. Если бы его не было, клик по тексту случайно закрыл бы окно. | `<div onclick="event.stopPropagation()">` |
| `<button class="close-btn" onclick="closeHero()">×</button>` | Кнопка-крестик в углу карточки. | Ещё один способ вызвать функцию закрытия окна. | `<button onclick="closeMenu()">X</button>` |
| `<img id="mImg" src="" alt="Hero Large">` | Картинка в карточке. Сейчас она пустая. | `src=""` пустой. Когда ты кликнешь на героя в таблице, JS возьмет его фотку и подставит ссылку сюда по `id="mImg"`. | `<img src="cat.jpg" alt="Кот">` |
| `<div id="mDet" class="modal-info"></div>` | Пустая коробка внутри карточки для текста (рост, вес, описание). | Опять же, JS найдет ее по `id="mDet"` и запихнет туда текст выбранного персонажа. | `<div id="resultBox"></div>` |
| `</div>` | Закрывает карточку `modal-content`. | - | *(Стандартный тег)* |
| `</div>` | Закрывает темный фон `modal`. | - | *(Стандартный тег)* |
| `</body>` | Закрывает видимую часть сайта. | HTML почти закончен! | *(Стандартный тег)* |
| `</html>` | Закрывает самый первый тег. Конец документа! | Дальше пустота. | *(Стандартный тег)* |

**Главный вывод:** Этот HTML файл ничего сам не считает и не ищет. Это просто "скелет" со множеством бейджиков (атрибуты `id` и `data-col`). Твоя главная задача в JavaScript-файле — научиться находить элементы по этим бейджикам (через `document.getElementById`) и менять их содержимое. 
